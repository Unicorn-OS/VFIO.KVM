---
- name: Blacklist GPU driver
  blockinfile:
    path: /etc/modprobe.d/blacklist-gpu.conf
    create: yes
    block: |
      blacklist amdgpu
      blacklist nouveau
      blacklist nvidiafb
      options nouveau modeset=0
  notify:
    - update initramfs
  when: ids.blacklist
  become: true
