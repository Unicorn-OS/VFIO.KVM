/*
https://docs.openstack.org/image-guide/convert-images.html
*/

img.example='disk'

to_Qcow(){
  qemu-img convert -f raw -O qcow2 $img.img $img.qcow2
}

convert_Xencfg(){
  sed -i 's/file:/tap:qcow2:/' *.cfg
  sed -i 's/disk.img/disk.qcow2/' *.cfg
}
