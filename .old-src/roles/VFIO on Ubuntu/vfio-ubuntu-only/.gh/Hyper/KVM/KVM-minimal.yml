- hostname:
    name: uni{{n}}

- name: packages
  apt:
    name: "{{ packages }}"
  vars:
    packages:
    - qemu-kvm
    - libvirt-daemon-system
    #- libvirt-daemon

# Now you can connect with Virt Manager!
